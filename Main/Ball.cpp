#include "Ball.hpp"

// Define the handleBall function for the Ball class (to be implemented by derived classes)
void Ball::handleBall(int runs, details &player) {
    // This is a pure virtual function, so it needs to be implemented in derived classes
    // This default implementation does nothing
}
